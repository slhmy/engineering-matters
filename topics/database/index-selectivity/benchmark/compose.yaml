name: index-selectivity

services:
  postgres:
    image: postgres:17.6-alpine
    environment:
      POSTGRES_DB: index_selectivity
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d index_selectivity"]
      interval: 1s
      timeout: 5s
      retries: 30
    ports:
      - "127.0.0.1:15437:5432"
    tmpfs:
      - /var/lib/postgresql/data
    volumes:
      - ./sql:/benchmark:ro
